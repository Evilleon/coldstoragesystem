<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ryq.coldstoragesystem.mapper.TransferMapper">

    <insert id="insertTransfer">
        insert into transfer(old_room,new_room,identifier,transfer_timestamp) value (#{oldRoom},#{newRoom},#{identifier},#{transferTimestamp})
    </insert>
</mapper>